<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adilegungor.hayvanbulmaca">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_owl"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnimalFinder">
        <activity android:name="com.adilegungor.hayvanbulmaca.GameActivity" />
        <activity android:name="com.adilegungor.hayvanbulmaca.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>